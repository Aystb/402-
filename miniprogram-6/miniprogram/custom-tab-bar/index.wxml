<!-- 自定义tab -->
<view class="functionBar">
  <view wx:for="{{list}}" wx:key="index" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab">
    <image class="{{item.className}}" src="{{selected === index ? item.selectedIconPath : item.iconPath}}"></image>
  </view>
</view>